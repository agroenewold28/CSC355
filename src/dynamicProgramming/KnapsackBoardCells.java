package dynamicProgramming;

// Created by 
public class KnapsackBoardCells {
    int weightOfCell;  // What weight does this column in the board correspond to.
    int bestValue;     // What do we think is the highest value we could fit in that weight.
    String items;      // What items does that bestValue correspond to.
}
